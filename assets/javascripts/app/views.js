App.ApplicationView = Ember.View.extend({
  templateName: 'application'
});

App.MeetingsView = Ember.View.extend({
  templateName: 'meetings'
});

App.MembersView = Ember.View.extend({
  templateName: 'members'
});
